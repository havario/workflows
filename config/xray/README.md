# Xray

```shell
bash <(curl -Ls https://github.com/233boy/Xray/raw/main/install.sh)
```

```shell
(crontab -l 2>/dev/null; echo "0 0 * * * /bin/bash -c '/usr/bin/bash <(curl -Ls https://raw.githubusercontent.com/havario/workflows/dev-next/config/xray/geodat.sh)'") | crontab -
```